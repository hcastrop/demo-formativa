import { FakeNgIfDirective } from './fake-ng-if.directive';

describe('FakeNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new FakeNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
